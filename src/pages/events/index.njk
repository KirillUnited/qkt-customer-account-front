---
layout: default.njk
title: 'My Saved Events'
description: 'These are events you have previously favourited.'
breadcrumbs: false
body_class: "tabs"
---

{% set filter=title|replace(' ', '_') %}
{% include "../../_includes/filter.njk" %}
<div class="products">
    <div class="products-list grid grid-col-5 grid-col-fill">
        {%- for item in events.data -%}
            {% CardProduct type="card-product", img=item.img, title=item.title, desc=item.desc, date=item.date, time=item.time %}
        {%- endfor -%}
    </div>
</div>
